import "./style.css";

const createShip = (length) => {
  let hit = [];
  let sunk = false;
  const isSunk = () =>{
   return this.length
  }
  return { length, sunk, hit };
};

const createGameBoard = (() => {
  const board = {}
  const receiveAttack = () => {};
    // game saves the position of the ships
    // 
  return {
      
  };
});

export {createShip, createGameBoard}